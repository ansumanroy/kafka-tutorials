plugins {
    id 'java'
    id 'application'
}

group = 'com.kafkatutorials.streams'
version = '1.0.0'

repositories {
    mavenCentral()
    maven {
        url 'https://packages.confluent.io/maven/'
    }
}

dependencies {
    // Kafka Streams
    implementation 'org.apache.kafka:kafka-streams:3.6.1'
    implementation 'org.apache.kafka:kafka-clients:3.6.1'
    
    // Streams test utilities
    testImplementation 'org.apache.kafka:kafka-streams-test-utils:3.6.1'
    
    // JSON processing
    implementation 'com.google.code.gson:gson:2.10.1'
    
    // Logging
    implementation 'org.slf4j:slf4j-api:2.0.9'
    implementation 'ch.qos.logback:logback-classic:1.4.11'
    
    // Testing
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.1'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

test {
    useJUnitPlatform()
    systemProperty 'KAFKA_BOOTSTRAP_SERVERS', System.getenv('KAFKA_BOOTSTRAP_SERVERS') ?: 'localhost:9092'
    testLogging {
        events "passed", "skipped", "failed"
        exceptionFormat "full"
        showStandardStreams = true
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

application {
    mainClass = 'com.kafkatutorials.streams.ktable.KTableDemo'
}

tasks.register('runKTable', JavaExec) {
    description = 'Run KTable demo'
    group = 'application'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.kafkatutorials.streams.ktable.KTableDemo'
    environment 'KAFKA_BOOTSTRAP_SERVERS', System.getenv('KAFKA_BOOTSTRAP_SERVERS') ?: 'localhost:9092'
}

tasks.register('runGlobalKTable', JavaExec) {
    description = 'Run GlobalKTable demo'
    group = 'application'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.kafkatutorials.streams.ktable.GlobalKTableDemo'
    environment 'KAFKA_BOOTSTRAP_SERVERS', System.getenv('KAFKA_BOOTSTRAP_SERVERS') ?: 'localhost:9092'
}
